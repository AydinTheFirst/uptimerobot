## Fullstack template

- React + Tailwind + Flowbite
- Express + MongoDB + Passport + SMTP

## Get Started

    # Run Frontend
    `cd client` > `yarn` > `yarn dev`

    # Run Backend
    `cd server` > `yarn` > `yarn dev`
